# Domain Attachment

The production domain is TBD.

When acquired:

1. Attach the domain in Vercel.
2. Set `PUBLIC_APP_URL`.
3. Set `BETTER_AUTH_URL`.
4. Update Stripe Checkout return URLs via environment.
5. Verify auth email links and checkout success/cancel redirects.

